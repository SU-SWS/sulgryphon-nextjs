import {ContactCardParagraph} from "@/lib/drupal/drupal";

const NodeReferenceCard = ({paragraph}: { paragraph: ContactCardParagraph }) => {
  return (
    <div>This references the branch page.</div>
  )
}

export default NodeReferenceCard;